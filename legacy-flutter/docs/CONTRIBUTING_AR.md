# المساهمة في صدى

شكراً لاهتمامك بالمساهمة في صدى! يوفر هذا المستند إرشادات وتعليمات للمساهمة في المشروع.

---

## 🤝 مدونة قواعد السلوك

- كن محترماً وشاملاً
- رحب بالوافدين الجدد وساعدهم على التعلم
- ركز على الملاحظات البناءة
- احترم وجهات النظر والتجارب المختلفة

---

## 🚀 البدء

### المتطلبات

- Flutter SDK 3.10.4+
- Dart SDK 3.10.4+
- Android Studio / VS Code
- Git

### الإعداد

1. **Fork المستودع**
   ```bash
   git clone https://github.com/yourusername/sada.git
   cd sada
   ```

2. **تثبيت التبعيات**
   ```bash
   flutter pub get
   ```

3. **إنشاء الكود**
   ```bash
   flutter pub run build_runner build --delete-conflicting-outputs
   ```

4. **إنشاء التوطين**
   ```bash
   flutter gen-l10n
   ```

5. **تشغيل التطبيق**
   ```bash
   flutter run
   ```

---

## 📝 سير عمل التطوير

### تسمية الفروع

- **ميزة**: `feature/feature-name`
  - مثال: `feature/add-file-sharing`
- **إصلاح خطأ**: `bugfix/issue-description`
  - مثال: `bugfix/fix-memory-leak`
- **إصلاح عاجل**: `hotfix/critical-issue`
  - مثال: `hotfix/security-patch`
- **توثيق**: `docs/documentation-update`
  - مثال: `docs/update-readme`

### تنسيق رسالة Commit

استخدم تنسيق conventional commits:

```
<type>(<scope>): <subject>

<body>

<footer>
```

**الأنواع:**
- `feat`: ميزة جديدة
- `fix`: إصلاح خطأ
- `docs`: تغييرات التوثيق
- `style`: تغييرات نمط الكود (التنسيق)
- `refactor`: إعادة هيكلة الكود
- `test`: إضافة اختبارات
- `chore`: مهام الصيانة

**أمثلة:**
```
feat(chat): إضافة تشفير الرسائل
fix(auth): حل مشكلة التحقق من PIN
docs(readme): تحديث تعليمات التثبيت
```

---

## 🏗️ نمط الكود

### دليل نمط Dart

- اتبع [Effective Dart](https://dart.dev/guides/language/effective-dart)
- استخدم `dart format` قبل الـ commit
- الحد الأقصى لطول السطر: 100 حرف
- استخدم أسماء متغيرات ذات معنى (تعليقات عربية للوضوح)

### تنظيم الملفات

```
lib/
├── core/
│   ├── database/
│   ├── network/
│   ├── router/
│   ├── security/
│   ├── services/
│   ├── theme/
│   ├── utils/
│   └── widgets/
└── features/
    ├── auth/
    ├── chat/
    ├── groups/
    ├── home/
    └── settings/
```

### اصطلاحات التسمية

- **الملفات**: `snake_case.dart`
- **الفئات**: `PascalCase`
- **المتغيرات**: `camelCase`
- **الثوابت**: `UPPER_SNAKE_CASE`
- **الخاص**: `_leadingUnderscore`

### تعليقات الكود

- استخدم التعليقات العربية لمنطق الأعمال (حسب تفضيل المستخدم)
- استخدم التعليقات الإنجليزية للشرح التقني
- وثّق الخوارزميات المعقدة
- اشرح "لماذا" وليس فقط "ماذا"

---

## 🧪 الاختبار

### تشغيل الاختبارات

```bash
# تشغيل جميع الاختبارات
flutter test

# تشغيل ملف اختبار محدد
flutter test test/features/chat/chat_test.dart

# تشغيل مع التغطية
flutter test --coverage
```

### كتابة الاختبارات

- **اختبارات الوحدة**: اختبر منطق الأعمال بمعزل عن غيرها
- **اختبارات Widget**: اختبر مكونات UI
- **اختبارات التكامل**: اختبر تدفقات المستخدم الكاملة

**مثال:**
```dart
test('يجب تشفير الرسالة بشكل صحيح', () {
  final service = EncryptionService(mockKeyManager);
  final encrypted = service.encryptMessage('Hello', sharedKey);
  expect(encrypted, isNotEmpty);
});
```

---

## 🌐 التوطين

### إضافة سلاسل جديدة

1. **أضف إلى ملفات ARB** (`l10n/app_en.arb` و `l10n/app_ar.arb`):
   ```json
   {
     "newKey": "New String",
     "@newKey": {
       "description": "Description of the string"
     }
   }
   ```

2. **إنشاء التوطين**:
   ```bash
   flutter gen-l10n
   ```

3. **استخدم في الكود**:
   ```dart
   Text(l10n.newKey)
   ```

### إرشادات الترجمة

- **الإنجليزية**: احترافية، واضحة، مختصرة
- **العربية**: عربية احترافية، واعية لـ RTL
- **الكردية**: نص لاتيني (ليس نص عربي)
- **Placeholders**: استخدم معاملات الدالة للقيم الديناميكية

---

## 🔒 اعتبارات الأمان

### قبل الإرسال

- ✅ لا توجد أسرار أو مفاتيح API مبرمجة
- ✅ جميع البيانات الحساسة في FlutterSecureStorage
- ✅ لا يوجد تسجيل للمعلومات الحساسة
- ✅ التحقق من المدخلات وتنظيفها
- ✅ معالجة الأخطاء للحالات الحدية

### مراجعة الأمان

التغييرات الحساسة للأمان تتطلب:
- مراجعة الكود من قبل الم maintainers
- قائمة تدقيق أمنية
- توثيق نموذج التهديد

---

## 📋 عملية Pull Request

### قبل الإرسال

1. **تحديث التوثيق**
   - تحديث README إذا لزم الأمر
   - تحديث الوثائق ذات الصلة في `docs/`
   - إضافة تعليقات الكود

2. **تشغيل الاختبارات**
   ```bash
   flutter test
   flutter analyze
   ```

3. **فحص Linting**
   ```bash
   flutter pub run custom_lint
   ```

4. **تنسيق الكود**
   ```bash
   dart format lib/
   ```

### قائمة PR

- [ ] الكود يتبع إرشادات النمط
- [ ] الاختبارات مضافة/محدثة
- [ ] التوثيق محدث
- [ ] سلاسل التوطين مضافة (إذا لزم الأمر)
- [ ] لا توجد أخطاء linter
- [ ] جميع الاختبارات تمر
- [ ] اعتبارات الأمان معالجة

### قالب وصف PR

```markdown
## الوصف
وصف موجز للتغييرات

## نوع التغيير
- [ ] إصلاح خطأ
- [ ] ميزة جديدة
- [ ] تغيير كاسر
- [ ] تحديث التوثيق

## الاختبار
كيف تم اختبار هذا؟

## لقطات الشاشة (إن أمكن)
أضف لقطات الشاشة هنا

## قائمة التحقق
- [ ] الكود يتبع إرشادات النمط
- [ ] الاختبارات مضافة/محدثة
- [ ] التوثيق محدث
```

---

## 🐛 الإبلاغ عن الأخطاء

### قالب تقرير الخطأ

```markdown
## الوصف
وصف واضح للخطأ

## خطوات إعادة الإنتاج
1. الخطوة الأولى
2. الخطوة الثانية
3. الخطوة الثالثة

## السلوك المتوقع
ما الذي يجب أن يحدث

## السلوك الفعلي
ما الذي يحدث فعلياً

## البيئة
- إصدار Flutter: [مثلاً، 3.10.4]
- إصدار Dart: [مثلاً، 3.10.4]
- الجهاز: [مثلاً، Android 13، Pixel 6]
- إصدار التطبيق: [مثلاً، 1.0.0]

## لقطات الشاشة
إن أمكن

## السجلات
إخراج السجل ذو الصلة
```

---

## 💡 طلبات الميزات

### قالب طلب الميزة

```markdown
## وصف الميزة
وصف واضح للميزة

## حالة الاستخدام
لماذا هذه الميزة مطلوبة؟

## الحل المقترح
كيف يجب أن يعمل هذا؟

## البدائل المدروسة
النهج الأخرى المدروسة

## سياق إضافي
أي معلومات أخرى ذات صلة
```

---

## 🎯 مجالات الأولوية

### أولوية عالية

- **تنفيذ Mesh الأصلي**: WiFi P2P و Bluetooth LE
- **بروتوكول الرسائل**: تنسيق رسالة Mesh والتوجيه
- **تكامل قاعدة البيانات**: Drift/Hive لتخزين الرسائل
- **تدقيقات الأمان**: مراجعة الكود واختبار الاختراق

### أولوية متوسطة

- **الاختبار**: اختبارات الوحدة، اختبارات التكامل
- **الأداء**: التحسين والتحليل
- **التوثيق**: توثيق API، دروس
- **إمكانية الوصول**: دعم قارئ الشاشة، تحسينات التباين

### أولوية منخفضة

- **تلميع UI**: الرسوم المتحركة، الانتقالات
- **ميزات إضافية**: مشاركة الملفات، الرسائل الصوتية
- **دعم المنصة**: تنفيذ iOS

---

## 📞 الحصول على المساعدة

### الموارد

- **GitHub Issues**: للأخطاء وطلبات الميزات
- **GitHub Discussions**: للأسئلة والمناقشات
- **التوثيق**: تحقق من مجلد `docs/` أولاً

### أسئلة؟

- افتح GitHub Discussion
- تحقق من المشاكل الموجودة
- راجع التوثيق

---

## 🙏 التقدير

المساهمون سيتم:
- إدراجهم في CONTRIBUTORS.md
- الإشادة بهم في ملاحظات الإصدار
- تقديرهم من قبل المجتمع!

---

## 📄 الترخيص

بالمساهمة، أنت توافق على أن مساهماتك ستُرخّص تحت رخصة MIT.

---

شكراً لمساهمتك في صدى! 🎉

