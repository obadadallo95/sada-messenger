# إعداد صلاحيات الكاميرا

تم إضافة صلاحيات الكاميرا المطلوبة لمسح رمز QR في التطبيق.

## ✅ التعديلات المنجزة

### Android (`android/app/src/main/AndroidManifest.xml`)
تم إضافة الصلاحيات التالية في بداية ملف `AndroidManifest.xml`:

```xml
<!-- Camera permissions for QR code scanning -->
<uses-permission android:name="android.permission.CAMERA" />
<uses-feature android:name="android.hardware.camera" android:required="false" />
<uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
```

### iOS (`ios/Runner/Info.plist`)
تم إضافة المفتاح التالي في ملف `Info.plist`:

```xml
<!-- Camera permissions for QR code scanning -->
<key>NSCameraUsageDescription</key>
<string>نحتاج إلى الوصول إلى الكاميرا لمسح رمز QR الخاص بأصدقائك</string>
```

## 📝 ملاحظات مهمة

1. **Android**: الصلاحيات مضافة تلقائياً عند تثبيت التطبيق، لكن يجب طلبها من المستخدم في وقت التشغيل (Runtime) - وهذا يتم تلقائياً في الكود.

2. **iOS**: يجب أن يكون `NSCameraUsageDescription` موجوداً في `Info.plist` وإلا سيرفض App Store التطبيق.

3. **Testing**: تأكد من اختبار صلاحيات الكاميرا على أجهزة حقيقية، لأن المحاكيات قد لا تدعم الكاميرا بشكل كامل.

## 🔧 التبعيات المضافة

- `qr_flutter`: لإنشاء وعرض رموز QR
- `mobile_scanner`: لمسح رموز QR من الكاميرا
- `permission_handler`: لإدارة صلاحيات الكاميرا
- `share_plus`: لمشاركة رمز QR

## ✅ الحالة

جميع التعديلات تمت بنجاح! التطبيق جاهز لاستخدام الكاميرا ومسح رموز QR.

