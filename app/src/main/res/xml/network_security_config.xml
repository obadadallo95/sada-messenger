<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- All traffic encrypted by default - no cleartext allowed in production -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Local mesh network: allow cleartext only for LAN/P2P direct connections -->
    <!-- Sada operates 100% offline - no external servers - only device-to-device -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Local subnet for mesh socket connections -->
        <domain includeSubdomains="false">192.168.0.0</domain>
    </domain-config>
</network-security-config>
